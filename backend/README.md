# Backend Placeholder

The MVP uses Supabase directly from the frontend with RLS and uses CLI scripts for admin workflows.

Future backend responsibilities:

- Secure invite-code RSVP lookup.
- Admin CSV exports through a web dashboard.
- Email invitations.
- Service-role operations.
- Webhook handling.
- Private server-side actions.

Never expose Supabase service role keys in frontend code.
